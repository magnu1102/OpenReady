export const APP_NAME = "OpenReady";

/** Build-time version from package.json — shown as the shell version badge. */
export const APP_VERSION = __APP_VERSION__;

/** @deprecated Replaced by the version badge; last consumers are removed in the shell revamp. */
export const APP_PHASE = "Phase 19 · Visual and text revamp";
