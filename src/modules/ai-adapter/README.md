# ai-adapter

Optional AI-assisted suggestions. Always opt-in, always bring-your-own-key, never required and never replaces the deterministic core.

**Phase:** placeholder, disabled. Lands in Phase 15.

**Public surface (planned):** `critiqueReadme(text, provider)`, `summarizeProject(result, provider)`.

Privacy-first: explicit content visibility, redaction rules and cost surfacing are part of the design.
